# Define the vectors
student_names <- c("Alice", "Bob", "Charlie", "David")
student_ages <- c(20, 21, 22, 20, 23)
data_structures_marks <- c(85, 90, 78, 92, 88)
data_strutures_marks <- data.frame(
	Name= student_names,
	Age = student_ages,
	DataStructuresMarks = data_structure_marks
)

cat("Student Information: \n")
print(students_data)
